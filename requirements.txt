# Core scientific stack
numpy==2.0.2
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.10.8

# Gradient boosting
lightgbm==4.6.0

# Causal inference
econml==0.16.0
causallib==0.10.0
scikit-uplift==0.5.1

# Tabular foundation models
tabpfn==7.1.1
tabicl==2.0.2
causalpfn==0.1.4

# PyTorch — CPU build (MPS on Apple Silicon, or replace index URL for CUDA)
# For NVIDIA GPU (CUDA 12.8):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch==2.9.1

# Jupyter
ipykernel==6.30.1

# Data loading
ucimlrepo==0.0.7
# pd.read_sas works without extras, but pyreadstat improves reliability for .sas7bdat
# pyreadstat
